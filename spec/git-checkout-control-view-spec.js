'use babel';

import GitCheckoutControlView from '../lib/git-checkout-control-view';

describe('GitCheckoutControlView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
